---
name: "Design a model, not deploy one"
tags: [negative, deploy-dataverse-schema, near-miss]
plugins: ["../../.."]
runs: 1
max_turns: 3
timeout_seconds: 120
---
Design a data model for a new Dataverse project that tracks customer onboarding tasks.
